# Replication Bundle

Run from project root:

```powershell
Rscript src/99_run_all.R
```

Outputs are regenerated in:
- `outputs/tables/`
- `outputs/figures/`
- `outputs/robustness/`

Then compile manuscript in `v1.3/` and copy final PDF to `v1.3/paper_writing_sample.pdf`.
